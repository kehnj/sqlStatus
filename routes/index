'use strict';

module.exports = [

    //directory path for web pages
    { path: '/{param*}',
      method: 'GET',
      handler: {
        directory: {
          path: 'public',
          index: true
        }
      }
    }

];
